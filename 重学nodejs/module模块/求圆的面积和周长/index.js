var circle = require('./circle');
const r = 4; 
// console.log("半径为4的圆" + "周长是 : " + circle.perimeter(r) + ' 面积是 : ' 
// 			+ circle.area(r) );
console.log(`半径为4的圆周长是 ：${circle.perimeter(r)} 面积是 ：${circle.area(r)}`);